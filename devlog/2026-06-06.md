# 开发日志 — 2026-06-06

## 今日完成

### 安全审查与改进（基于全栈代码审查报告）


#### 第 1 轮：P0 底线修复 ✅

| # | 改进项 | 文件 | 说明 |
|---|--------|------|------|
| A | 数据库竞态条件修复 | `backend/db.js` | 增加 Promise guard (`dbReady`)，多个并发请求共享同一个初始化 Promise，防止创建多个数据库实例导致数据丢失 |
| B | 数据库崩溃保护 | `backend/db.js` | `scheduleSave()` / `saveNow()` 中 `try/catch` 包裹写盘操作，防止磁盘满/权限错误崩溃进程 |
| C | 数据库句柄泄漏 | `backend/db.js` | `queryAll()` 中 `stmt.free()` 放入 `try/finally`，确保 step() 抛异常时句柄仍能释放 |
| D | JWT 过期时间 | `backend/middleware/auth.js` | `jwt.sign()` 增加 `expiresIn: '365d'`，token 不再是永不过期 |
| E | JWT 硬编码密钥 | `backend/middleware/auth.js` | 移除 fallback secret，未配置 `JWT_SECRET` 时 `process.exit(1)` |
| F | 邀请码安全 | `backend/utils/invite.js` | `Math.random()` → `crypto.randomInt()` |
| G | Error Boundary | `frontend/src/components/ErrorBoundary.jsx` | 新增 Error Boundary 组件，捕获未处理渲染错误 |
| H | Auth 严格限流 | `backend/index.js` | Auth 路由独立限流器 20次/15min |

#### 第 2 轮：P1 质量加固 ✅

| # | 改进项 | 文件 | 说明 |
|---|--------|------|------|
| I | 死代码清理 | `backend/routes/records.js` | 删除重复的 DELETE /all 路由 |
| J | 死代码清理 | `backend/routes/statistics.js` | 删除重复的 return 死代码块 |
| K | JSON.parse 安全 | `backend/routes/statistics.js` | `safeParse()` 包装 + null 检查，单条脏数据不崩溃整个查询 |

#### 第 3 轮：P1/P2 锦上添花 ✅

| # | 改进项 | 文件 | 说明 |
|---|--------|------|------|
| L | deploy.sh 加固 | `deploy.sh` | 禁止 root；git clone 替代 cp -r；nginx reload；健康检查 |
| M | PM2 优化 | `ecosystem.config.js` | max_restarts 10→50；kill_timeout 10s |
| N | Nginx 安全头 | `nginx.conf.example` | X-Frame-Options/X-Content-Type/XSS/Referrer |

#### 第 4 轮：better-sqlite3 迁移 ✅

| # | 改进项 | 文件 | 说明 |
|---|--------|------|------|
| O | 数据库驱动替换 | `backend/package.json` | `sql.js` → `better-sqlite3@12.10.0` |
| P | 数据库层重写 | `backend/db.js` | 同步 API，真实 WAL 模式，删除防抖/导出/句柄释放代码，-80行 |
| Q | 路由层适配 | `auth.js/baby.js/records.js/sleep-timer.js/statistics.js/index.js` | `await getDb()` → `getDb()`，删除所有 `saveNow()` 调用 |

## 技术变更统计

```
Round 1-2: 12 files, +195/-157
Round 3:    4 files, +67/-35
Round 4:    9 files, +462/-142
Total:     25 files touched across all rounds
```

## Git 提交记录

```
f8c648c refactor: migrate sql.js -> better-sqlite3
f9183c3 docs: add devlog for 2026-06-06 security review and improvements
9d4f131 fix: round 3 improvements - deploy, nginx, pm2, json parse safety
0108a21 fix(security): P0 fixes - race condition, JWT expiry, crypto invite, error boundary
3028914 feat: 新增辅食记录功能（含过敏追踪）
eaa4c7a Initial commit: baby tracker - full stack baby feeding & care tracking app
```

## 当前状态

- **P0 问题**：5/5 清零 ✅
- **P1 核心**：4/4 完成 ✅
- **P1/P2 锦上添花**：4/6 完成 ✅ (Vite split/前端小修跳过)
- **better-sqlite3 迁移**：完成 ✅
- **GitHub 同步**：已推送 ✅
- ****下一步**：新服务器部署
